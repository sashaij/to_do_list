export class Task {
    constructor (title, dueDate, priority, index, description) {
        this.title = title;
        this.dueDate = dueDate;
        this.priority = priority;
        this.index = index;
        this.description = description;
    }
}
